{
  "private": true,
  "type": "module",
  "scripts": {
    "tunet:build": "node build.mjs",
    "tunet:build:watch": "node build.mjs --watch",
    "tunet:deploy:lab": "node build.mjs --deploy",
    "tunet:lab:screenshot": "echo 'Use Playwright MCP to capture lab screenshots at locked breakpoints'",
    "test": "vitest run"
  },
  "dependencies": {
    "@playwright/test": "^1.59.1"
  },
  "devDependencies": {
    "@playwright/mcp": "^0.0.70",
    "esbuild": "^0.25.0",
    "jsdom": "^29.0.1",
    "vitest": "^3.1.0"
  }
}
