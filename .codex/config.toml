# Project-scoped Codex config for HA implementation_10
# Inherits from ~/.codex/config.toml, overrides only what's needed

approval_policy = "on-request"

[sandbox_workspace_write]
network_access = true

# Playwright MCP — optional, requires @playwright/mcp installed locally
# [mcp_servers.playwright]
# command = "npx"
# args = ["@playwright/mcp@0.0.70", "--headless"]
